.title{
  font-weight: 400;
  font-size: 14px;
  color: rgba(255, 255, 255, 0.5);
  margin-left: 30px;
}

.headLine-component{
  background: #43436B;
}

.headline-container{
  min-height: 80px;
  display: flex;
  align-items: center;
}

.buttons{
  margin-left: auto;
}

.headline-escape-btn{
  outline: none;
  background: none;
  color: rgba(255, 255, 255, 0.9);

  &:hover{
    text-decoration: underline;
    transition: .3s;
  }
}